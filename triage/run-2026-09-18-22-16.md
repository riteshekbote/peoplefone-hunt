
===== TRIAGE 2026-09-18 22:16:59 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/scope*.yml"[90m 1 match[0m
[0m✱ [0mGlob "**/inventory/**"[90m 1 match[0m
[0m✱ [0mGlob "**/peoplefone*"[90m 1 match[0m
Let me check the scope file to validate these leads properly.
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/peoplefone.md
Now I have full context. Here is the triage:

---

## Lead Triage — 7-Question Gate

### LEADS 1–18: Pipeline Defect (triage.yml:45 `grep "[UNVALIDATED]"`)

All `[CHANGED]`, `[NEW]`, `[LEARN]`, `[NEXT]` items referencing the `triage.yml:45` literal-grep defect and its fix.

| Q | Answer |
|---|--------|
| Q1 Scope? | **NO** — `.github/workflows/triage.yml` is the hunters' own CI/CD pipeline, not a peoplefone GmbH asset (scope: company infrastructure, VoIP, APIs, portals, domains). |
| Q2 Reachable? | N/A — out of scope at Q1. |
| Q3 Impact? | **NO** — defect affects only the bot's lead-pipeline starvation; zero security impact on any peoplefone customer or service. |
| Q4 Provable? | Yes (read-only grep analysis), but irrelevant — OOS. |
| Q5 Novel? | N/A — not a security finding. |
| Q6 Rejected list? | N/A — not applicable to tooling bugs. |
| Q7 Accept? | **NO** — reasonable triager would reject a tooling bug in the hunter's own repo as out-of-scope for the peoplefone program. |

**Verdict: INVALID**
**Reason:** This is a defect in the hunters' own workflow pipeline, not a vulnerability in peoplefone GmbH infrastructure. Zero program relevance.

---

### LEAD 19 (final `[NEXT]` item): OAuth Open-Redirect / Login-CSRF — `auth.peoplefone.com`

> `GET /oauth/authorize?client_id=1&response_type=token&redirect_uri=https://attacker.example/cb&scope=openid` → `Set-Cookie: redirect_uri=<attacker>; Max-Age=34560000`

| Q | Answer |
|---|--------|
| Q1 Scope? | **YES** — `auth.peoplefone.com` is a peoplefone GmbH domain under "all company-owned infrastructure... and domains operated by peoplefone GmbH." |
| Q2 Reachable? | **YES** — OAuth `/oauth/authorize` is publicly accessible (no auth required to initiate flow); 302 preserving attacker `redirect_uri` and 404 setting `redirect_uri` cookie both confirm unauthenticated reachability. |
| Q3 Impact? | **YES** — Open redirect chained into OAuth flow enables: (1) authorization code/token interception by redirecting callback to attacker-controlled endpoint, (2) login-CSRF via forced authentication context, (3) persistent cookie injection (1-year expiry) affecting future sessions. |
| Q4 Provable? | **YES** — single passive `GET` request, response headers observed. Non-destructive, read-only HTTP. |
| Q5 Novel? | **YES** — 50+ frozen cycles in knowledge base with no evidence of prior report; first confirmed triage 2026-09-08. |
| Q6 Rejected list? | **NO** — not DoS, not info disclosure, not clickjacking-without-PoC, not CSRF-on-anonymous-forms. This is a functional OAuth redirect_uri validation bypass with real exploitability. |
| Q7 Accept? | **YES** — real OAuth vulnerability, independently triaged VALID 7.4 (9.1 conditional) across multiple cycles, in scope, provable non-invasively. |

**Verdict: VALID**

**Minimal read-only proof steps:**
1. `GET https://auth.peoplefone.com/oauth/authorize?client_id=1&response_type=token&redirect_uri=https://attacker.example/cb&scope=openid`
2. Observe `302` with `Location` header preserving attacker `redirect_uri`, or `404` with `Set-Cookie: redirect_uri=https://attacker.example/cb; Max-Age=34560000` (httponly, secure).
3. Replay with warm portal session (`portal.peoplefone.ch` → login → auth flow) to confirm full redirect chain preservation.

**Impact:** OAuth token/account takeover via redirect_uri manipulation. Attacker captures authorization code or implicit token on their callback server.

**CVSS 3.1:** AV:N/AC:H/PR:N/UI:R/S:U/C:H/I:H/A:N → **7.4 (High)**; escalates to **9.1 (Critical)** if `response_type=token` yields direct token issuance without additional validation.

**Reporting channel:** `bugs.olivermaicher.eu` (Oliver Maicher private program, per `scope.yml:4`).
