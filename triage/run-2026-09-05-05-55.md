
===== TRIAGE 2026-09-05 05:55:43 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
[0m✱ [0mGlob "**/*scope*"[90m 1 match[0m
[0m✱ [0mGlob "**/*leads*"[90m 1 match[0m
[0m✱ [0mGlob "**/*inventory*"[90m 0 matches[0m
Let me check the repository for the hunt bot leads and scope information.
[0m→ [0mRead inventory
[0m→ [0mRead scope.yml
[0m→ [0mRead new-leads.txt
[0m→ [0mRead probe-results.md
[0m→ [0mRead probe-results.txt
[0m→ [0mRead leads
[0m→ [0mRead inventory/peoplefone.md
[0m→ [0mRead triage
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/reposcan-peoplefone.md
[0m→ [0mRead triage-output.txt
[0m→ [0mRead leads/lead-nemotron3.md[90m [offset=355][0m
Now let me check the latest triage output and remaining lead files for additional context.
[0m
[0m# [0mTodos
[✓] Consolidate distinct hypotheses from all model lead files
[•] Triage each hypothesis against 7-Question Gate
[ ] Write final triage output
[0m
Now I have the full picture. Let me consolidate the distinct hypotheses across all lead files and triage each.
[0m# [0mTodos
[✓] Consolidate distinct hypotheses from all model lead files
[✓] Triage each hypothesis against 7-Question Gate
[✓] Write final triage output
[0m
---

# TRIAGE OUTPUT — peoplefone GmbH Bug Bounty
## 2026-09-05 | Hunt Bot Leads Validation

**Source files:** `leads/lead-bigpickle.md`, `leads/lead-nemotron3.md`, `probe-results.md`
**Models with substantive output:** bigpickle (9 rounds), nemotron3 (11 rounds), laguna/ling3/longcat/mimo (empty/stale)

**Consolidated distinct hypotheses from all models:**

| # | Hypothesis | Class | Asset | Confidence | Cross-model |
|---|---|---|---|---|---|
| 1 | Configuration API IDOR/BOLA on {identifier} CRUD | IDOR | configuration-api.peoplefone.com | 85 | bigpickle+nemotron3 |
| 2 | OAuth arbitrary redirect_uri / implicit-token theft | AUTH | auth.peoplefone.com | 78 | bigpickle+nemotron3 |
| 3 | SSRF via 5 callback-accepting endpoints | SSRF | api+call-api+config-api | 78 | bigpickle+nemotron3 |
| 4 | BOLA on SMS messageId (cross-tenant SMS disclosure) | IDOR | api.peoplefone.com | 80 | bigpickle+nemotron3 |
| 5 | Queue API cross-tenant agent login/logout | BUSLOGIC | call-api.peoplefone.com | 65 | bigpickle |

**Rejected/parked (not triaged):** GraphQL introspection (no GraphQL found), Subdomain takeover (Cloudflare CDN dominated), Support portal info disclosure (low confidence, low value), External Routing deprecated API (sub-variant of SSRF #3)

---

## LEAD 1: Configuration API IDOR/BOLA on {identifier} CRUD across 8 resource types

**Assets:** `configuration-api.peoplefone.com/customer/voip/v1/{users,groups,ivrs,queues,numbers,smart-routings,callforwarding,manual-routing}/{identifier}`

| Gate | Verdict | Reason |
|---|---|---|
| Q1 Scope | **YES** | configuration-api.peoplefone.com is a peoplefone GmbH VoIP/PBX API — falls under "VoIP platforms, SIP services, PBX systems, APIs" in scope.yml |
| Q2 Reachable | **PARTIAL** | 401 enforced on live endpoints; requires valid bearer token. Not unauth-accessible. "Low-priv" may apply if token-scope isolation is weak (spec says "user must be part of account bound to bearer token") |
| Q3 Impact | **YES** | Cross-tenant PBX takeover — SIP credentials (sipUserName), physical addresses, emails, DID routing, call forwarding rules, IVR menus, queue configs, smart routing webhooks. Full CRUD including DELETE. CRITICAL severity |
| Q4 Non-invasive proof | **NO** | Spec analysis only. 235KB OpenAPI spec reveals 8 resource types with numeric sequential identifiers (20023, 20024, 2, 2000) and an explicit authorization boundary statement, but no exploitation demonstrated. Requires valid token to confirm cross-tenant access. |
| Q5 Novel | **YES** | No evidence of prior report. Cross-model convergence (bigpickle+nemotron3) strengthens confidence. |
| Q6 Not rejected | **YES** | Not on always-rejected list. |
| Q7 Reasonable triager | **NO** | Spec-only IDOR hypotheses without PoC or demonstrated cross-tenant access are routinely rejected. Most triagers require at least a single request showing foreign tenant data. |

### **VERDICT: HOLD**

**Reason:** Highest-confidence hypothesis (85, cross-model convergence) with CRITICAL potential impact, but cannot be validated non-invasively. Token acquisition via portal.peoplefone.ch is the sole blocker. The authorization boundary statement in the spec ("user must be part of account bound to bearer token") could indicate proper enforcement — or could be aspirational documentation. **Action:** Human operator must create test account, obtain bearer token, then run `GET /customer/voip/v1/users/{own_id+1}` to confirm or deny cross-tenant access. If confirmed → report immediately (CRITICAL).

---

## LEAD 2: OAuth arbitrary redirect_uri / implicit-token theft (client_id=1)

**Assets:** `auth.peoplefone.com/oauth/authorize` + `/oauth/token` + `portal.peoplefone.ch`

| Gate | Verdict | Reason |
|---|---|---|
| Q1 Scope | **YES** | auth.peoplefone.com + portal.peoplefone.ch are in-scope ("All company-owned infrastructure… customer portals… domains operated by peoplefone GmbH") |
| Q2 Reachable | **YES** | OAuth authorize endpoint is publicly accessible without any authentication. GET `https://auth.peoplefone.com/oauth/authorize?client_id=1&redirect_uri=https://evil.com/callback&response_type=code&scope=openid` returns HTTP 302 → `/de_CH/login` with attacker redirect_uri PRESERVED. Fully unauthenticated reachability confirmed. |
| Q3 Impact | **YES** | If client_id=1 is a public/PKCE client: full ATO of any portal user (call recordings, CDR, PBX config, billing, SMS, PII). Even if confidential: confirmed open-redirect primed on login flow + login-CSRF primitive. Severity: CRITICAL (conditional on client type) |
| Q4 Non-invasive proof | **YES** | Passively confirmed: 302 redirect preserving arbitrary redirect_uri through login flow. Implicit (`response_type=token`) AND PKCE (`code_challenge`+`code_challenge_method=S256`) params accepted and preserved. `/oauth/token` returns HTTP 405 on GET (endpoint exists). All observable without any login or token. |
| Q5 Novel | **YES** | Bigpickle reversed its own prior REJECTED verdict when new evidence appeared. No evidence of prior report. Live endpoint confirmed 2026-09-05 00:16 UTC. |
| Q6 Not rejected | **YES** | Not on always-rejected list. OAuth redirect_uri validation bypass is a classic accepted finding class. |
| Q7 Reasonable triager | **YES** | The redirect_uri bypass is demonstrable via passive HTTP observation. The 302 preserving an attacker-controlled redirect_uri through a real login page is a concrete, exploitable primitive. Open redirect on a login flow is accepted by most programs. If public client is confirmed → full ATO. |

### **VERDICT: VALID**

**Minimal read-only proof steps:**
1. `GET https://auth.peoplefone.com/oauth/authorize?client_id=1&redirect_uri=https://attacker.example.com/callback&response_type=code&scope=openid&state=test123` → observe HTTP 302 → `/de_CH/login` with `redirect_uri=https://attacker.example.com/callback` preserved in the login form action/redirect chain
2. Confirm implicit flow: `response_type=token` accepted and preserved through same 302
3. Confirm PKCE flow: `code_challenge=abc&code_challenge_method=S256` accepted and preserved through same 302
4. `GET https://auth.peoplefone.com/oauth/token` → HTTP 405 (confirms live token-exchange endpoint)

**Impact:** Open redirect primed on real login flow + login-CSRF; full ATO if public/PKCE client. Attack vector: attacker sends crafted authorize URL to victim → victim logs in → code/token delivered to attacker redirect_uri.

**CVSS 3.1:** AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N → **9.1 CRITICAL** (if public client confirmed; else AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:L/A:N → **7.4 HIGH** for open-redirect + login-CSRF)

**Reporting channel:** bugs.olivermaicher.eu (Oliver Maicher private program) — report + PoC + contact + payment. Include the HTTP 302 trace showing redirect_uri preservation and the 405 on /oauth/token.

---

## LEAD 3: SSRF via 5 callback-accepting endpoints with zero host validation

**Assets:**
- `api.peoplefone.com/customer/sms/v1/sms/messages` (callbackUrl)
- `call-api.peoplefone.com/customer/smart-routing/v1/smart-routings/{id}/webhook` (url)
- `call-api.peoplefone.com/customer/uacsta/v1_beta/device/call` (callbackUrl)
- `call-api.peoplefone.com/customer/uacsta/v1_beta/device/monitorStart` (callbackUrl + monitoringCallbackUrl)
- `configuration-api.peoplefone.com/customer/voip/v1/external-number-lookup` (webhookUrl — also forwards customer-configured Authorization/X-API-Key headers)

| Gate | Verdict | Reason |
|---|---|---|
| Q1 Scope | **YES** | All5 endpoints on in-scope peoplefone GmbH subdomains (VoIP platforms, APIs) |
| Q2 Reachable | **PARTIAL** | All endpoints require valid bearer token (401 confirmed live). Not unauth-accessible. |
| Q3 Impact | **YES** | Cloud metadata/IAM keys theft via 169.254.169.254; internal network pivot to SIP/PBX; External Number Lookup SSRF exfiltrates customer-configured Authorization/X-API-Key headers to attacker URL; uaCSTA streams real-time call events (OFF_HOOK, CONNECTED, TERMINATED) + SIP usernames + phone numbers. CRITICAL severity. |
| Q4 Non-invasive proof | **NO** | Spec analysis only. OpenAPI specs confirm `type: string format: uri` with zero host/scheme validation, but no actual callback reachability or internal-IP blocking behavior demonstrated. Requires token to POST to endpoints and verify callback delivery. |
| Q5 Novel | **YES** | 5 distinct endpoints; External Number Lookup header-forwarding variant is unusual. No evidence of prior report. |
| Q6 Not rejected | **YES** | Not on always-rejected list. |
| Q7 Reasonable triager | **NO** | Spec-only SSRF hypotheses without demonstrated callback delivery are insufficient. Triagers typically reject "the spec says it accepts a URL" without proof the server actually fetches it. |

### **VERDICT: HOLD**

**Reason:** High-confidence hypothesis (78) with CRITICAL impact, but requires token to demonstrate callback reachability. The External Number Lookup header-forwarding variant (forwards customer-configured Authorization + X-API-Key to attacker URL) is particularly noteworthy if confirmed. **Action:** Human operator must obtain token, then POST to SMS endpoint with `callbackUrl=https://<collaborator>/callback`, observe callback delivery, then test `http://169.254.169.254/latest/meta-data/` variant.

---

## LEAD 4: BOLA on SMS messageId (cross-tenant SMS disclosure)

**Assets:** `api.peoplefone.com/customer/sms/v1/sms/messages/{messageId}` + `/status/{messageId}`

| Gate | Verdict | Reason |
|---|---|---|
| Q1 Scope | **YES** | api.peoplefone.com is in inventory; SMS API documented as public "free for all developers" |
| Q2 Reachable | **PARTIAL** | Requires valid bearer token (401 enforced). SMS API is public ("free for all developers") which lowers token acquisition barrier, but still requires authentication. |
| Q3 Impact | **YES** | Cross-tenant SMS disclosure — phone numbers, message content, delivery status, timestamps. PII exposure. HIGH/CRITICAL severity depending on message sensitivity. |
| Q4 Non-invasive proof | **NO** | Spec analysis only. GET `/messages/{messageId}` and `/status/{messageId}` return full message content+recipient per spec, but messageId format/predictability and tenant-scope enforcement are unproven without a token. |
| Q5 Novel | **YES** | No evidence of prior report. |
| Q6 Not rejected | **YES** | Not on always-rejected list. |
| Q7 Reasonable triager | **NO** | Spec-only. Needs demonstration that a messageId from tenant A is accessible with tenant B's token. |

### **VERDICT: HOLD**

**Reason:** Strong hypothesis (80) but subsumed by broader SSRF/IDOR hypotheses and requires token for proof. The "free for all developers" claim makes token acquisition potentially trivial via portal self-service registration. **Action:** After token acquisition, GET `/customer/sms/v1/sms/messages` to capture messageId format, then test sequential/neighbor IDs for cross-tenant response.

---

## LEAD 5: Queue API cross-tenant agent login/logout business logic

**Assets:** `call-api.peoplefone.com/customer/queue/v1/agents/login` + `/agents/logout`

| Gate | Verdict | Reason |
|---|---|---|
| Q1 Scope | **YES** | call-api.peoplefone.com is a peoplefone GmbH call management API |
| Q2 Reachable | **PARTIAL** | Requires bearer token (401 enforced) |
| Q3 Impact | **YES** | Cross-tenant call center disruption — force agents offline, reroute calls, impact business continuity. HIGH severity. |
| Q4 Non-invasive proof | **NO** | Spec-only. POST accepts agent identifier + queueScope, but spec is silent on membership-vs-boundary validation. No demonstration that cross-tenant agent identifiers are accepted. |
| Q5 Novel | **YES** | No evidence of prior report. |
| Q6 Not rejected | **YES** | Not on always-rejected list. |
| Q7 Reasonable triager | **NO** | Weakest evidence class (65 confidence). Spec-silent on validation — this is speculative. Even bigpickle PARKED this in favor of the OAuth chain. |

### **VERDICT: INVALID**

**Reason:** Lowest-confidence hypothesis (65), spec-silent on the critical validation question, PARKED by originating model. Cross-tenant agent login/logout is plausible but entirely speculative without token-based proof. A reasonable triager would reject a finding where the spec doesn't even describe the membership validation that would need to be bypassed.

---

## SUMMARY TABLE

| # | Hypothesis | Verdict | CVSS 3.1 | Action |
|---|---|---|---|---|
| 1 | Configuration API IDOR/BOLA {identifier} CRUD | **HOLD** | CRITICAL if confirmed | Create test account → probe /users/{own_id±1} |
| 2 | OAuth redirect_uri bypass (client_id=1) | **VALID** | 9.1 CRITICAL (or 7.4 HIGH) | **Report to bugs.olivermaicher.eu** with HTTP 302 trace |
| 3 | SSRF via 5 callback endpoints | **HOLD** | CRITICAL if confirmed | Create test account → POST with collaborator URL |
| 4 | SMS BOLA on messageId | **HOLD** | HIGH/CRITICAL if confirmed | Create test account → test neighbor messageIds |
| 5 | Queue API agent login/logout | **INVALID** | N/A | Park / drop — too speculative |

**Total VALID for immediate report:** 1 (OAuth redirect_uri bypass)
**Total HOLD pending token:** 3 (Configuration IDOR, SSRF, SMS BOLA)
**Total INVALID:** 1 (Queue API business logic)
[0m← [0mWrite triage/triage-run-2026-09-05-12-00.md
Wrote file successfully.
[0m
Triage complete. Written to `triage/triage-run-2026-09-05-12-00.md`.

**Results:**

| # | Hypothesis | Verdict | CVSS |
|---|---|---|---|
| 1 | Configuration API IDOR/BOLA {identifier} CRUD | **HOLD** | CRITICAL if confirmed |
| 2 | OAuth redirect_uri bypass (client_id=1) | **VALID** | 9.1 CRITICAL |
| 3 | SSRF via 5 callback endpoints | **HOLD** | CRITICAL if confirmed |
| 4 | SMS BOLA on messageId | **HOLD** | HIGH/CRITICAL if confirmed |
| 5 | Queue API agent login/logout | **INVALID** | N/A |

**1 VALID for immediate report:** OAuth redirect_uri bypass — the authorize endpoint at `auth.peoplefone.com/oauth/authorize?client_id=1` accepts arbitrary `redirect_uri` and preserves it verbatim through the login flow (confirmed via passive HTTP 302 observation). If client_id=1 is a public/PKCE client (strongly suggested by implicit + PKCE param acceptance), this is full ATO of any portal user. Report to **bugs.olivermaicher.eu** with the 302 trace + `/oauth/token` 405 evidence.

**3 HOLD:** All converge on a single blocker — bearer token acquisition via `portal.peoplefone.ch`. The self-service registration endpoint is confirmed LIVE. One test account unlocks all three simultaneously.

**1 INVALID:** Queue API business logic — too speculative (65 confidence, spec-silent on the validation question it depends on).
